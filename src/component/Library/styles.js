import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,

    },
    cardDisplay: {
        flexDirection: "row",
        flexWrap: "wrap"
    }
});
export default styles